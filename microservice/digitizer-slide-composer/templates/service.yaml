apiVersion: v1
kind: Service
metadata:
  name: {{ include "digitizer-slide-composer.fullname" . }}
  labels:
    {{- include "digitizer-slide-composer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digitizer-slide-composer.selectorLabels" . | nindent 4 }}
